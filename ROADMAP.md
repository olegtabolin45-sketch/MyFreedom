# 🚀 AETERNA ANALYTICS — Roadmap

> Аналог Snowball Income, но лучше. Инструмент для анализа капитала и построения пассивного дохода.
> Принципы: **безопасность с первого дня**, **горизонтальное масштабирование**, **чистый Apple-style интерфейс**.

---

## 📊 Текущее состояние (что уже есть)

- ✅ Фронтенд: лендинг + формы регистрации/входа (`index.html`), калькулятор трека (`dashboard.html`)
- ✅ Бэкенд: FastAPI с эндпоинтами `register / login / user/status / onboarding` (`server.py`)
- ✅ JWT-авторизация + bcrypt-хэширование паролей
- ✅ Хранилище: Trino + Iceberg

---

## ⚠️ КРИТИЧНО исправить до любого нового функционала

Это нужно закрыть **до** написания новых фич — иначе будем масштабировать дыры.

- [x] **SQL-инъекции.** ✅ Все запросы переведены на параметризованные (`cursor.execute(query, params)`), `escape_sql` удалена.
- [x] **Секреты в коде.** ✅ `JWT_SECRET` вынесен в переменные окружения, `.env` в `.gitignore`, есть `.env.example`.
- [x] **CORS.** ✅ Ограничен списком `ALLOWED_ORIGINS`; фронтенд отдаётся с того же origin, что и API.
- [x] **Trino — не для пользователей.** ✅ Пользователи и цели перенесены в **PostgreSQL** (драйвер pg8000, контейнер `aeterna-db`, схема — миграции Alembic). Trino зарезервирован для аналитики капитала (этап 4).
- [x] **Нет rate limiting.** ✅ Добавлен лимитер для register/login (в памяти; Redis-вариант — этап 2).
- [x] **Логи с чувствительными данными.** ✅ Переход на `logging`, убрана утечка деталей ошибок клиенту.

---

## 🏗️ Этап 1 — Фундамент архитектуры

- [x] Разделить монолит `server.py` на модули. ✅ Пакет `app/`: `config`, `db`, `security`, `rate_limit`, `schemas`, `logging_config`, `routers/` (frontend, auth, users). `server.py` — тонкая точка входа.
- [x] Подключить PostgreSQL + миграции (Alembic). ✅ PostgreSQL подключён; Alembic (через pg8000) с начальной миграцией `0001`, применяется автоматически при старте контейнера.
- [x] Файл зависимостей `requirements.txt`. ✅ (`bcrypt==4.0.1`, pg8000 и др.)
- [x] Централизованная конфигурация из `.env`. ✅ Модуль `app/config.py` (pydantic-settings — опционально позже).
- [x] Docker + `docker-compose.yml`. ✅ `Dockerfile` + compose (app + postgres, схема применяется автоматически). Trino/redis добавим на соответствующих этапах.
- [x] Логирование. ✅ Модуль `app/logging_config.py` (structlog/loguru — улучшение позже).

## 🔐 Этап 2 — Безопасность (углублённо)

- [x] Refresh-токены + короткоживущие access-токены. ✅ Access — JWT на 15 мин с `jti`; refresh — opaque-токен с ротацией. Эндпоинты `/api/refresh`, `/api/logout`.
- [x] Хранение refresh-токенов и blacklist в **Redis**. ✅ `app/redis_client.py`; refresh в `refresh:<token>` (TTL 30 дней), blacklist access по `jti` в `bl:<jti>`. Контейнер `aeterna-redis` (appendonly).
- [ ] Подтверждение email при регистрации
- [ ] Сброс пароля по email
- [x] Двухфакторная аутентификация (2FA / TOTP). ✅ `app/two_factor.py` (pyotp + QR), миграция `0003` (поля `totp_secret`, `is_2fa_enabled`), роутер `/api/2fa/setup|enable|disable`, проверка кода в login. Фронт: поле кода при `requires_2fa`. (UI включения 2FA с QR в личном кабинете — отдельная задача.)
- [x] Заголовки безопасности (HSTS, CSP, X-Frame-Options) через middleware. ✅ `app/middleware.py` (`SecurityHeadersMiddleware`): CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy на все ответы.
- [ ] Защита от CSRF
- [x] Аудит-лог действий пользователя. ✅ Таблица `audit_log` (миграция `0002`), модуль `app/audit.py`; пишутся register, login_success/failed, token_refresh, logout, onboarding с IP и User-Agent.
- [x] Валидация и санитизация **всех** входных данных. ✅ `app/schemas.py`: `extra="forbid"` (запрет лишних полей), обрезка пробелов, лимиты длин, паттерн username, лимит пароля 72 байта (под bcrypt), whitelist валют и риск-профилей, верхние границы числовых полей онбординга.

## 🛡️ Этап 3 — Защита от DDoS и нагрузки

> Важно: полноценная защита от DDoS строится на уровне инфраструктуры, а не только кода.

- [ ] **CDN + WAF** (Cloudflare / подобное) — первая линия защиты от DDoS, фильтрация трафика
- [ ] Reverse-proxy (Nginx) с rate limiting и таймаутами
- [ ] Кэширование тяжёлых аналитических запросов в Redis
- [ ] Connection pooling к БД
- [ ] Защита от ботов (капча на регистрации/логине)
- [ ] Мониторинг аномального трафика

## 📈 Этап 4 — Основной функционал (аналитика капитала)

- [x] Онбординг: цели, валюта, горизонт, риск-профиль. ✅ UI онбординга в Apple-стиле (валюта, риск-профиль, капитал, пополнение, целевой доход, горизонт), маршрутизация после входа по `is_onboarded`, отправка в `/api/onboarding`. Заодно исправлен баг: лендинг не скрывался при входе (`!important` в `.landing-mode`).
- [~] **Портфель**: добавление активов. 🟡 Импорт брокерского отчёта Т-Банка (.xlsx): парсер (`app/broker_import.py`) извлекает позиции (секция 3.1) и сделки (1.1/1.3); таблицы `portfolio_positions`/`portfolio_trades` (миграция 0005); загрузка через `POST /api/portfolio/import` (drag&drop на вкладке «Общее»), отображение активов и сделок. Ручное добавление и брокерские API — далее.
- [~] Интеграция с источниками котировок (API бирж). 🟡 MOEX ISS (РФ, бесплатно, без ключа) — `app/quotes.py`, кэш 30 мин; в `GET /api/portfolio` подмешиваются живые цены, считается текущая стоимость портфеля и доли позиций. Иностранные котировки + FOREX и ЗПИФ-борды — далее.
- [ ] Расчёт средней цены, учёт комиссий, сплитов
- [~] Калькулятор сложного процента и прогноз трека. 🟡 В дашборде: помесячная симуляция роста капитала по риск-профилю, прогноз капитала и % достижения цели (правило 4%).
- [~] Дашборд (главная страница пользователя). 🟡 Концепт: профиль, KPI-карточки, анимированный area-график роста (с подписями осей), кольцо достижения цели, график реальной покупательной способности по странам, баннер утверждения цели; данные читаются/пишутся в PostgreSQL (`GET /api/goals`, `POST /api/goals/approve`). Будет дорабатываться.
- [~] Поправка на инфляцию. 🟡 В онбординге — выбор до 3 стран проживания; на дашборде — прогноз покупательной способности по прогнозной инфляции выбранных стран.
- [ ] **Календарь выплат**: дивиденды, купоны
- [ ] Расчёт налогов по юрисдикциям
- [ ] Бенчмаркинг доходности (TWR/MWR vs индексы)
- [ ] Дашборд с графиками (распределение, динамика)
- [ ] Экспорт отчётов (PDF/Excel)

## ⚡ Этап 5 — Масштабирование

- [ ] Stateless-приложение (вся сессия в Redis) → можно поднимать N инстансов
- [ ] Балансировщик нагрузки
- [ ] Очереди для тяжёлых задач (Celery + Redis/RabbitMQ): загрузка котировок, отчёты
- [ ] Реплики БД для чтения
- [ ] Health-checks и graceful shutdown
- [x] CI/CD pipeline (GitHub Actions). ✅ `.github/workflows/ci.yml`: на каждый PR/push в master поднимает PostgreSQL + Redis, применяет миграции Alembic и прогоняет pytest.
- [ ] Контейнеризация + оркестрация (Kubernetes — когда вырастет нагрузка)

## 🧪 Этап 6 — Качество и наблюдаемость

- [x] Юнит- и интеграционные тесты (`pytest`), покрытие ключевой логики. ✅ Каталог `tests/` (заголовки безопасности, валидация, цикл аутентификации, 2FA), флаг `RATE_LIMIT_ENABLED` для тестов, `requirements-dev.txt`. 20 тестов проходят.
- [x] Линтеры и форматтеры (`ruff`, `black`). ✅ Конфиг в `pyproject.toml` (line-length 100, правила E/W/F/I/UP/B), весь код приведён к стилю; проверки `ruff check` и `black --check` встроены в CI. (`mypy` — позже.)
- [ ] Метрики (Prometheus) + дашборды (Grafana)
- [ ] Трейсинг ошибок (Sentry)
- [ ] Документация API (FastAPI даёт Swagger из коробки)

## 🎨 Этап 7 — Интерфейс и продукт

- [ ] Перенести фронтенд на современный фреймворк (React/Vue) — когда логики станет много
- [ ] Сохранить нынешний минималистичный Apple-style дизайн
- [ ] Адаптивная вёрстка (мобильные)
- [ ] Тёмная тема
- [ ] Личный кабинет с полноценным дашбордом

---

## 🎯 Приоритеты на ближайшее время

1. Закрыть блок **«КРИТИЧНО исправить»** (особенно SQL-инъекции и секреты)
2. Завести `requirements.txt` + `.gitignore` + `.env`
3. Перенести пользователей с Trino на PostgreSQL
4. Начать рефакторинг монолита в модули

---

*Документ живой — обновляем по мере продвижения. Каждый пункт → отдельная ветка и PR.*
